{% extends 'base.html.twig' %}

{% block javascripts %}
 {{ parent() }}
 <script src="{{ asset('js/search/searchBar.js') }}"></script>
{% endblock %}


{% block title %}Movie index{% endblock %}

{% block body %}

<div id="search-bar" data-search-url="{{ path('app_movie_search') }}">
    <input type="text" id="search-input" placeholder="Rechercher des films">
    <ul id="search-results"></ul>
</div>


    {# <h1>Movie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Icon</th>
                <th>Description</th>
                <th>Themes</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for movie in movies %}
            <tr>
                <td>{{ movie.id }}</td>
                <td>{{ movie.name }}</td>
                <td>{{ movie.icon }}</td>
                <td>{{ movie.description }}</td>
                <td>{{ movie.themes }}</td>
                <td>
                    <a href="{{ path('app_movie_show', {'id': movie.id}) }}">show</a>
                    <a href="{{ path('app_movie_edit', {'id': movie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> #}

    <a href="{{ path('app_movie_new') }}">Create new</a>

{% endblock %}
