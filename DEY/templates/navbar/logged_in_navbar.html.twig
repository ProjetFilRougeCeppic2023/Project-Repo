{# templates/navbar/logged_in_navbar.html.twig #}

<nav class="navbar navbar-expand-lg bg-light" data-bs-theme="light">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">
			<img class="logo" src="{{ asset('img/DEY-cond copy.png') }}" alt="Logo" height="30">
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsable-nav" aria-controls="collapsable-nav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div id="collapsable-nav" class="collapse navbar-collapse">
			<ul id="nav-list" class="nav navbar-nav navbar-right">
				<li class="nav-item{% if current_route == 'home' %} active-link{% endif %}">
                    <a class="nav-link" href="{{ path('home') }}">
                        Home
                        {% if current_route == 'home' %}
                            <span class="visually-hidden">(current)</span>
                        {% endif %}
                    </a>
                </li>
				<li class="nav-item {% if current_route == 'create' %} active-link{% endif %}">
					<a class="nav-link" href="{{ path('create') }}">
						Create
						{% if current_route == 'create' %}
							<span class="visually-hidden">(current)</span>
						{% endif %}
					</a>
				</li>
				<li id="search-bar" class="nav-item">
					<form class="d-flex">
						<input id="search-input" class="form-control me-2" type="search" placeholder="Search">
						<button id="search-button" class="btn btn-secondary" type="submit">Search</button>
					</form>
				</li>
				<li class="nav-item {% if current_route == 'dashboard' %} active-link{% endif %}">
					<a class="nav-link" href="{{ path('dashboard') }}">
						Dashboard
						{% if current_route == 'dashboard' %}
							<span class="visually-hidden">(current)</span>
						{% endif %}
					</a>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Dropdown</a>
					<div class="dropdown-menu dropdown-menu-end">
						<a class="dropdown-item text-nowrap" href="#">Profile</a>
						<a class="dropdown-item text-nowrap" href="#">Settings</a>
						<div class="dropdown-divider"></div>
						<a class="dropdown-item text-nowrap" href="#">Logout</a>
					</div>
				</li>
			</ul>
		</div>
	</div>
</nav>
